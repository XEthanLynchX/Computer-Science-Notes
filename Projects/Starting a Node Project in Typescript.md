1. Make sure to have NVM installed on your machine and use the version you want to use 
2. npm install --save-dev typescript (installs ts as dev depend)
3. npx tsc --init (Creates tsconfig)
4. Create a .vscode folder for launching with Debugger (Determines what file is used by the debugger)
5. Create a ReadMe for program descriptions 
6. Add everything not meant to be public in .gitignore (e.g. node modules, env, etc )
7. 